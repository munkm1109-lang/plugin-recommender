# Release Notes

## v0.1.0

Initial beta release.

- Adds `plugin-recommender` Codex plugin.
- Bundles a Korean plugin catalog snapshot.
- Provides a `plugin-recommender` skill for explicit plugin recommendation requests.
- Runs locally without telemetry or external account connection.
